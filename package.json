{
  "name": "op-reconnect",
  "version": "1.0.0",
  "description": "OP: ReConnect — an individual streaming game for ARMY. The city went dark; every stream brings a piece of it back.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "vite": "^5.4.0"
  }
}
